# donation-dashboard
A full-stack donation tracking dashboard built using React and Node.js. The application displays total donations, number of donors, and recent donation data fetched from a backend API. The frontend is built with React and the backend uses Express to serve donation data.
